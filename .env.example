# Copy to .env to make these permanent:  cp .env.example .env
# docker compose reads .env automatically.

MLFLOW_PORT=5000
API_PORT=8000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000

# Classroom credentials only. Never commit real ones.
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
